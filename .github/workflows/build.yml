name: macOS DMG Build and Release

on:
  push:
    branches:
      - master # Запускать рабочий процесс при пуше в ветку master

permissions:
  contents: write # Разрешить запись в репозиторий для создания релиза

jobs:
  build-macos:
    name: 🎉 Сборка macOS DMG
    runs-on: macos-latest # Использовать последнюю версию macOS ранера

    steps:
      - name: 🔍 Проверка кода
        uses: actions/checkout@v4 # Извлечь код из репозитория

      - name: 📦 Установка Flutter SDK
        uses: subosito/flutter-action@v2 # Использовать стабильную версию действия Flutter
        with:
          channel: stable # Использовать стабильный канал Flutter
          architecture: x64 # Для архитектуры x64

      - name: 📦 Получение зависимостей Flutter
        run: flutter pub get # Установить зависимости проекта

      - name: 🔨 Сборка приложения macOS (релизная версия)
        run: flutter build macos --release # Собрать приложение Flutter для macOS в режиме релиза

      - name: 🗂 Поиск собранного приложения
        id: find_app # Присвоить ID этому шагу для доступа к его выводам
        run: |
          # Собранное приложение обычно находится в build/macos/Build/Products/Release/
          # Нам нужно найти его точное имя (например, 'Runner.app' или 'YourAppName.app')
          APP_DIR="build/macos/Build/Products/Release/"
          # Найти первый каталог, заканчивающийся на ".app"
          APP_NAME=$(find "$APP_DIR" -maxdepth 1 -type d -name "*.app" | head -n 1)
          
          if [ -z "$APP_NAME" ]; then
            echo "Ошибка: Не удалось найти пакет .app в $APP_DIR"
            exit 1
          fi
          
          # Получить только базовое имя пакета приложения (например, "Runner.app")
          APP_BASENAME=$(basename "$APP_NAME")
          
          echo "Найденное приложение: $APP_BASENAME"
          # Установить выводы для использования в последующих шагах
          echo "app_path=$APP_NAME" >> "$GITHUB_OUTPUT"
          echo "app_basename=$APP_BASENAME" >> "$GITHUB_OUTPUT"

      - name: ⬇️ Установка create-dmg CLI
        run: npm install --global create-dmg # Установить утилиту create-dmg глобально через npm

      - name: 💾 Создание образа DMG
        env:
          # Установка ad-hoc подписи, чтобы create-dmg не искал реальный сертификат
          CODE_SIGN_IDENTITY: "-" 
        run: |
          set -x # Включить отладочное логирование для более подробного вывода
          # Используем утилиту create-dmg напрямую
          # Добавлена опция --no-sign для пропуска подписи кода
          create-dmg "${{ steps.find_app.outputs.app_path }}" \
            --output MyFlutterApp.dmg \
            --volume-name "MyFlutterApp" \
            --window-pos "200 120" \
            --window-size "800 600" \
            --icon-size 100 \
            --app-drop-link-x 600 \
            --app-drop-link-y 185 \
            --no-sign # <-- Изменена эта строка для отключения подписи кода
          # Опционально: --background 'assets/dmg-background.png' # Если у вас есть фоновое изображение
          
          # Убедимся, что файл DMG существует
          if [ ! -f MyFlutterApp.dmg ]; then
            echo "Ошибка: Файл MyFlutterApp.dmg не был создан."
            exit 1
          fi

      - name: 🌐 Публикация в GitHub Release
        uses: svenstaro/upload-release-action@v2 # Действие для загрузки файлов в релиз GitHub
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Токен для аутентификации в GitHub
          file: MyFlutterApp.dmg # Файл DMG, созданный на предыдущем шаге
          tag: latest-mac-release # Тег релиза. Вы можете изменить его на динамический, например, v1.0.0
          overwrite: true # Перезаписывать существующий актив, если тег уже существует
          body: Автоматически собранное macOS приложение. # Описание релиза
          draft: false # Опубликовать релиз сразу (true для черновика)
          prerelease: false # Не помечать как предварительный релиз
