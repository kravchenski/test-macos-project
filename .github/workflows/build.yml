name: macOS DMG Build and Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-macos:
    name: 🎉 Build macOS DMG
    runs-on: macos-latest

    steps:
      - name: 🔍 Checkout код
        uses: actions/checkout@v4

      - name: 📦 Установить Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          architecture: x64

      - name: 📦 Получить зависимости
        run: flutter pub get

      - name: 🔨 Собрать приложение macOS (release)
        run: flutter build macos --release

      - name: 🧰 Установить Node.js и create-dmg
        run: |
          brew install node
          npm install -g create-dmg

      - name: 🗂 Подготовить структуру DMG
        run: |
          mkdir -p dmg_build
          ls -la build/macos/Build/Products/Release/
          cp -R build/macos/Build/Products/Release/*.app dmg_build/

      - name: 💾 Создать DMG образ
        run: |
          cd dmg_build || exit 1
          APP_NAME=$(ls *.app)
          echo "Найдено приложение: $APP_NAME"

          create-dmg \
            --volname "MyFlutterApp" \
            --volume-icon "../macos/Runner/Assets.xcassets/AppIcon.appiconset/icon.icns" \
            --window-pos 200 120 \
            --window-size 800 600 \
            --icon-size 100 \
            --icon "$APP_NAME" 200 190 \
            --hide-extension "$APP_NAME" \
            --app-drop-link 600 185 \
            MyFlutterApp.dmg \
            "$APP_NAME"

      - name: 🌐 Опубликовать как GitHub Release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: dmg_build/MyFlutterApp.dmg
          tag: latest-mac-release
          overwrite: true
          body: Автоматически собранное macOS приложение.
          draft: false
          prerelease: false